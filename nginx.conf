server {
    listen 80;
    server_name _;

    # www.jsinnovia.com is kept only as a compatibility entry point.
    # 308 preserves the HTTP method and request body, including Elynea POST submissions.
    return 308 https://jsinnovia.com$request_uri;
}
